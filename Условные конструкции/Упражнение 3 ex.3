#include <stdio.h>
  
int main()
{
    int m = 5;
    if (m >= 4 && m <= 14)
    {
        printf("within range [4..14]", m);
    }
    else
    {
        printf("out of range", m);
    }
    return 0;
}
